export const metadata = {
    title: "My Profile | Karyaa",
    description: "Manage your Karyaa account settings and preferences.",
};

export default function ProfileLayout({ children }) {
    return <>{children}</>;
}
